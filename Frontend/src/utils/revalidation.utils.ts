export const pathRevalidationConfirmationMsg =
    "When a path gets revalidated, the next time a user visits the associated page, it will be rebuilt on the server before being served. Consequently, the response time might be longer if data fetching is associated.";
